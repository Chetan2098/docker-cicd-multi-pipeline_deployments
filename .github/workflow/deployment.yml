name: Build and Deploy to Kubernetes

# Trigger conditions
on:
  # Allow manual trigger only
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - production

# Environment variables
env:
  REGISTRY: docker.io
  IMAGE_NAME: test-local-app
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}

jobs:
  # Job 1: Build Docker Image
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./app/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Job 2: Deploy to Kubernetes
  deploy:
    name: Deploy to Kubernetes
    runs-on: ubuntu-latest
    needs: build
    # Only deploy on push to main/master or manual trigger
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Verify Kubernetes connectivity
        run: |
          echo "Testing Kubernetes connection..."
          kubectl cluster-info
          kubectl get nodes
          echo "Connection successful!"

      - name: Get current deployments
        run: |
          echo "Current deployments:"
          kubectl get deployments
          echo "Current services:"
          kubectl get services
          kubectl get nodes
          echo "Connection successful!"
        run: |
      - name: Get current deployments..."
        run: |
          echo "Current deployments:"rom the build job
          kubectl get deployments }}/${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest"
          echo "Current services:"
          kubectl get servicese: $IMAGE"
          
      - name: Deploy to Kubernetes new image
        run: |ctl set image deployment/cicd-demo \
          echo "Deploying application..."
            --record
          # Get the full image name from the build job
          IMAGE="${{ env.REGISTRY }}/${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest"
          kubectl rollout status deployment/cicd-demo --timeout=5m
          echo "Deploying image: $IMAGE"
          me: Verify deployment
          # Update deployment with new image
          kubectl set image deployment/cicd-demo \
            cicd-demo=$IMAGE \nt cicd-demo -o wide
            --record
          echo "Pod Status:"
          # Wait for rolloutl app=cicd-demo
          kubectl rollout status deployment/cicd-demo --timeout=5m
          echo "Service Status:"
      - name: Verify deploymenticd-demo-service
        run: |
          echo "Deployment Status:"ation
          kubectl get deployment cicd-demo -o wide
          echo ""== Application Deployment Successful ==="
          echo "Pod Status:"
          kubectl get pods -l app=cicd-demo
          echo ""ort: 5000 (internal) -> 30001 (NodePort)"
          echo "Service Status:"
          kubectl get service cicd-demo-service -o wide

      - name: Display access information
        run: |
          echo "=== Application Deployment Successful ==="
          echo ""tu-latest
          echo "Service: cicd-demo-service"
          echo "Port: 5000 (internal) -> 30001 (NodePort)"
          echo ""
          kubectl get service cicd-demo-service -o wide
        uses: actions/checkout@v4
  # Job 3: Testing (optional)
  test: name: Set up Python
    name: Run Testsns/setup-python@v4
    runs-on: ubuntu-latest
    needs: build-version: '3.10'

    steps:me: Install dependencies
      - name: Checkout code
        uses: actions/checkout@v4-upgrade pip
          pip install -r app/requirements.txt
      - name: Set up Python
        uses: actions/setup-python@v4
        with:|
          python-version: '3.10'ion tests..."
          # Add your test commands here
      - name: Install dependencies --tb=short || echo "No tests found"
        run: |
          python -m pip install --upgrade pip
          pip install -r app/requirements.txt
    name: Send Notifications
      - name: Run testsest
        run: |ild, deploy]
          echo "Running application tests..."
          # Add your test commands here
          python -m pytest app/ -v --tb=short || echo "No tests found"
      - name: Deployment completed
  # Job 4: Notification
  notify: if [ "${{ job.status }}" == "success" ]; then
    name: Send Notificationsnt pipeline completed successfully!"
    runs-on: ubuntu-latestion is now running on Kubernetes"
    needs: [build, deploy]ia NodePort: http://localhost:30001"
    if: always()
            echo "❌ Deployment pipeline failed!"
    steps:  echo "Check logs for details"
      - name: Deployment completed
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Deployment pipeline completed successfully!"
            echo "Application is now running on Kubernetes"
            echo "Access via NodePort: http://localhost:30001"
          elseployment Summary
            echo "❌ Deployment pipeline failed!"
            echo "Check logs for details"
          fiCommit: ${{ github.sha }}
          - Author: ${{ github.actor }}
      - name: Create deployment summary
        if: always()${{ job.status }}
        run: |
          cat > deployment-summary.txt << EOF
          # Deployment Summary
          - Repository: ${{ github.repository }}
          - Branch: ${{ github.ref }}
          - Commit: ${{ github.sha }}@v3
          - Author: ${{ github.actor }}
          - Timestamp: $(date)mary
          - Status: ${{ job.status }}t
          EOF          cat deployment-summary.txt      - name: Upload deployment summary        if: always()        uses: actions/upload-artifact@v3        with:          name: deployment-summary          path: deployment-summary.txt